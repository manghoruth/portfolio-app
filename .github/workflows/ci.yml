name: My first pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v2
    - name: Build image
      run: docker build -t manghoruth/ciportfolio .

    - name: List images
      run: docker images
   
  